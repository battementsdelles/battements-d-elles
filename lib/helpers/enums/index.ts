export enum LoadingState {
  PENDING = 'PENDING',
  FULFILLED = 'FULFILLED',
  FAILED = 'FAILED',
  IDLE = 'IDLE',
}

export enum Providers {
  GOOGLE = 'google',
  CREDENTIALS = 'credentials',
}

export enum AlertType {
  ERROR = 'ERROR',
  INFO = 'INFO',
  SUCCESS = 'SUCCESS',
  WARNING = 'WARNING',
}
